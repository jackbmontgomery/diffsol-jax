{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"diffsol-jax","text":"<p>JAX wrapper around diffsol, a Rust ODE solver library.</p> <p>diffsol-jax exposes diffsol's ODE solvers via <code>jax.ffi</code> so they can be called inside <code>jax.jit</code> and differentiated with <code>jax.grad</code>. You write a right-hand-side function in Python; the library lowers it to a DiffSL source string, compiles it once, and caches the result — subsequent calls skip recompilation and update parameters in place. Gradients are computed via diffsol's discrete adjoint, wrapped with <code>jax.custom_vjp</code>.</p> <pre><code>import jax\njax.config.update(\"jax_enable_x64\", True)\n\nimport jax.numpy as jnp\nfrom diffsol_jax import ODEProblem\n\ndef lotka_volterra(t, y, p):\n    x, yy = y[0], y[1]\n    alpha, beta, delta, gamma = p[0], p[1], p[2], p[3]\n    return (alpha * x - beta * x * yy, delta * x * yy - gamma * yy)\n\nparams = jnp.array([1.5, 1.0, 0.75, 3.0])\ny0     = jnp.array([1.0, 0.5])\n\nproblem = ODEProblem(lotka_volterra, y0=y0, params=params)\n\nt_span = jnp.array([0.0, 10.0])\nts, ys = jax.jit(lambda p: problem.solve(p, t_span))(params)\n# ts: float64[200],  ys: float64[200, 2]\n</code></pre> <p>Getting started — install and first solve. API reference — full public API. Benchmarks — forward and gradient timing vs diffrax.</p>","path":["diffsol-jax"],"tags":[]},{"location":"benchmarks/","level":1,"title":"Benchmarks","text":"<p>All benchmarks compare diffsol-jax against diffrax on the same ODE problems, same tolerances (<code>rtol=atol=1e-8</code>), and the same hardware (Apple M-series). Each benchmark picks the most appropriate diffrax solver for the problem type.</p>","path":["Benchmarks"],"tags":[]},{"location":"benchmarks/#forward-solve","level":2,"title":"Forward solve","text":"<p>200 output times, wall-clock time per solve call after JIT warm-up.</p> System diffsol-jax diffrax Speedup Lotka-Volterra (non-stiff) 0.08 ms 0.23 ms (Dopri5) 2.92x Van der Pol μ=1000 (stiff) 0.5 ms 65 ms (Kvaerno5) ~120x <p>BDF's variable-order stepping gives a large advantage on stiff problems. On non-stiff systems explicit methods like Dopri5 are competitive.</p> <pre><code>uv run python benchmarks/bench_forward.py\nuv run python benchmarks/bench_stiff.py\n</code></pre>","path":["Benchmarks"],"tags":[]},{"location":"benchmarks/#gradient-parameter-fitting","level":2,"title":"Gradient / parameter fitting","text":"<p>500 Adam steps, <code>n_times=50</code>, Lotka-Volterra with Tsit45 + discrete adjoint.</p> System diffsol-jax diffrax Speedup Lotka-Volterra (Tsit45+AD) 184.9 ms / 500 steps 2882.4 ms / 500 steps 15.59x <p>Both solvers converge to the same parameters (<code>max |p_err| = 0.0175</code>). The speedup comes from diffsol running entirely in compiled Rust/LLVM — gradients avoid JAX's tracing overhead on the inner solver loop.</p> <pre><code>uv run python benchmarks/bench_grad.py\n</code></pre>","path":["Benchmarks"],"tags":[]},{"location":"getting-started/","level":1,"title":"Getting started","text":"","path":["Getting started"],"tags":[]},{"location":"getting-started/#requirements","level":2,"title":"Requirements","text":"<ul> <li>Rust toolchain (stable)</li> <li>Python &gt;= 3.12</li> <li>C++17 compiler</li> <li>LLVM 20 (required for adjoint/gradient support)</li> </ul> <p>On macOS with Homebrew:</p> <pre><code>brew install llvm\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#installation","level":2,"title":"Installation","text":"<pre><code>uv sync\nuv run maturin develop --release\n</code></pre> <p>Note</p> <p><code>.cargo/config.toml</code> sets <code>LLVM_SYS_201_PREFIX</code> to <code>/opt/homebrew/opt/llvm</code>. If LLVM 20 is installed elsewhere, update that path before running <code>maturin develop</code>.</p>","path":["Getting started"],"tags":[]},{"location":"getting-started/#first-forward-solve","level":2,"title":"First forward solve","text":"<p>Enable 64-bit floats — diffsol-jax requires f64:</p> <pre><code>import jax\njax.config.update(\"jax_enable_x64\", True)\n\nimport jax.numpy as jnp\nfrom diffsol_jax import ODEProblem\n</code></pre> <p>Define the RHS as a plain Python function returning a tuple of scalars, one per state component:</p> <pre><code>def lotka_volterra(t, y, p):\n    x, yy = y[0], y[1]\n    alpha, beta, delta, gamma = p[0], p[1], p[2], p[3]\n    return (alpha * x - beta * x * yy, delta * x * yy - gamma * yy)\n</code></pre> <p>Create example arrays for the initial state and parameters, then build the solver. The values in <code>y0</code> are baked into the compiled DiffSL source; <code>params</code> is used only to trace the RHS and set default parameter values:</p> <pre><code>params = jnp.array([1.5, 1.0, 0.75, 3.0])\ny0     = jnp.array([1.0, 0.5])\n\nproblem = ODEProblem(lotka_volterra, y0=y0, params=params)\n</code></pre> <p>Call <code>problem.solve</code> under <code>jax.jit</code>. The first call compiles; subsequent calls reuse the cached module and update parameters via <code>set_params</code>:</p> <pre><code>t_span = jnp.array([0.0, 10.0])\nts, ys = jax.jit(lambda p: problem.solve(p, t_span))(params)\n# ts: float64[200]    — uniformly spaced output times\n# ys: float64[200, 2] — solution at each time\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#computing-gradients","level":2,"title":"Computing gradients","text":"<p><code>jax.grad</code> works out of the box. The VJP uses diffsol's discrete adjoint; the compiled LLVM module is cached, so only the first gradient step pays compilation cost:</p> <pre><code>def loss(p):\n    ts, ys = problem.solve(p, t_span)\n    return jnp.sum(ys ** 2)\n\ngrad = jax.grad(loss)(params)\n</code></pre> <p>Gradients w.r.t. <code>t_span</code> return zeros. Gradient w.r.t. <code>y0</code> is computed internally but not returned — <code>y0</code> is baked into the DiffSL source at construction time.</p>","path":["Getting started"],"tags":[]},{"location":"getting-started/#where-to-go-next","level":2,"title":"Where to go next","text":"<ul> <li>API reference — full details on <code>ODEProblem</code>, solver options, and type aliases.</li> <li>Benchmarks — forward and gradient timing vs diffrax.</li> </ul>","path":["Getting started"],"tags":[]},{"location":"markdown/","level":1,"title":"Markdown in 5min","text":"","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#headers","level":2,"title":"Headers","text":"<pre><code># H1 Header\n## H2 Header\n### H3 Header\n#### H4 Header\n##### H5 Header\n###### H6 Header\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#text-formatting","level":2,"title":"Text formatting","text":"<pre><code>**bold text**\n*italic text*\n***bold and italic***\n~~strikethrough~~\n`inline code`\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#links-and-images","level":2,"title":"Links and images","text":"<pre><code>[Link text](https://example.com)\n[Link with title](https://example.com \"Hover title\")\n![Alt text](image.jpg)\n![Image with title](image.jpg \"Image title\")\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#lists","level":2,"title":"Lists","text":"<pre><code>Unordered:\n\n- Item 1\n- Item 2\n  - Nested item\n\nOrdered:\n\n1. First item\n2. Second item\n3. Third item\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#blockquotes","level":2,"title":"Blockquotes","text":"<pre><code>&gt; This is a blockquote\n&gt; Multiple lines\n&gt;&gt; Nested quote\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#code-blocks","level":2,"title":"Code blocks","text":"<pre><code>```javascript\nfunction hello() {\n  console.log(\"Hello, world!\");\n}\n```\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#tables","level":2,"title":"Tables","text":"<pre><code>| Header 1 | Header 2 | Header 3 |\n|----------|----------|----------|\n| Row 1    | Data     | Data     |\n| Row 2    | Data     | Data     |\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#horizontal-rule","level":2,"title":"Horizontal rule","text":"<pre><code>---\nor\n***\nor\n___\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#task-lists","level":2,"title":"Task lists","text":"<pre><code>- [x] Completed task\n- [ ] Incomplete task\n- [ ] Another task\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#escaping-characters","level":2,"title":"Escaping characters","text":"<pre><code>Use backslash to escape: \\* \\_ \\# \\`\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#line-breaks","level":2,"title":"Line breaks","text":"<pre><code>End a line with two spaces  \nto create a line break.\n\nOr use a blank line for a new paragraph.\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"api/","level":1,"title":"API overview","text":"<p>The main entry point is <code>ODEProblem</code>. Construct it with your RHS function, an initial state, and an example parameter vector; it compiles the problem once and exposes a <code>solve</code> callable that is compatible with <code>jax.jit</code> and <code>jax.grad</code>.</p>","path":["API reference","API overview"],"tags":[]},{"location":"api/#public-symbols","level":2,"title":"Public symbols","text":"Symbol Kind Description <code>ODEProblem</code> class Compiled ODE problem; exposes a <code>solve</code> method. <code>ODE_RHS</code> type alias <code>Callable</code> — type annotation for RHS functions. <code>ODE_SOLVER</code> type alias <code>Literal[\"bdf\", \"tsit45\", \"esdirk34\", \"tr_bdf2\"]</code> — solver names.","path":["API reference","API overview"],"tags":[]},{"location":"api/#solvers","level":2,"title":"Solvers","text":"<p>Pass the <code>ode_solver=</code> argument to <code>ODEProblem</code> to select a solver.</p> <code>ode_solver=</code> Type Adjoint <code>\"bdf\"</code> (default) BDF (implicit, variable-order) BDF <code>\"tsit45\"</code> Tsitouras 4(5) (explicit) Tsit45 <code>\"esdirk34\"</code> ESDIRK3(4) (implicit) BDF (fallback) <code>\"tr_bdf2\"</code> TR-BDF2 (implicit) BDF (fallback) <p>BDF and Tsit45 use their own solver for both forward and backward passes. ESDIRK34 and TR-BDF2 fall back to BDF for the adjoint — their implicit adjoint solvers fail to converge on non-trivial problems.</p> <pre><code>problem = ODEProblem(rhs, y0=y0, params=params, ode_solver=\"tsit45\")\n</code></pre>","path":["API reference","API overview"],"tags":[]},{"location":"api/diffsol_jax/","level":1,"title":"<code>diffsol_jax</code>","text":"","path":["API reference","diffsol_jax"],"tags":[]},{"location":"api/diffsol_jax/#diffsol_jax.ODEProblem","level":2,"title":"ODEProblem","text":"<pre><code>ODEProblem(rhs: Callable[[Float[Array, ''], Float[Array, ' state'], Float[Array, ' params']], Float[Array, ' state']], y0: Float[Array, ' state'], params: Float[Array, ' params'], n_times: int = 200, ode_solver: ODE_SOLVER = 'bdf')\n</code></pre> <p>An ODE problem compiled from a Python RHS function.</p> <p>Wraps a user-supplied right-hand-side function, lowers it to a <code>DiffSL &lt;https://martinjrobins.github.io/diffsl/&gt;</code>_ source string, and compiles it for use with :func:<code>jax.jit</code> and :func:<code>jax.grad</code>.</p> <p>The compiled solver is exposed via the :attr:<code>solve</code> method, which accepts parameters and a time span and returns solution trajectories.</p> <p>Compile an ODE problem from a Python RHS function.</p> <p>Parameters:</p> Name Type Description Default <code>rhs</code> <code>Callable[[Float[Array, ''], Float[Array, ' state'], Float[Array, ' params']], Float[Array, ' state']]</code> <p>Right-hand-side function <code>rhs(t, y, p) -&gt; tuple[float, ...]</code>. Must return a tuple of scalars, one per state component.  Supports elementwise JAX arithmetic, common unary functions (<code>sin</code>, <code>exp</code>, …), and scalar indexing of <code>y</code> and <code>p</code>.</p> required <code>y0</code> <code>Float[Array, ' state']</code> <p>Initial state vector.  Shape determines <code>n_state</code>.  Values are baked into the compiled DiffSL source; gradient w.r.t. <code>y0</code> is not returned by :attr:<code>solve</code>.</p> required <code>params</code> <code>Float[Array, ' params']</code> <p>Example parameter vector used to trace <code>rhs</code> and infer <code>n_params</code>.  The values set the DiffSL default parameters and are overridden at solve time.</p> required <code>n_times</code> <code>int</code> <p>Number of output time points (uniformly spaced between <code>t_span[0]</code> and <code>t_span[1]</code>).  Defaults to <code>200</code>.</p> <code>200</code> <code>ode_solver</code> <code>ODE_SOLVER</code> <p>Which solver to use for the forward pass.  One of <code>\"bdf\"</code> (default), <code>\"tsit45\"</code>, <code>\"esdirk34\"</code>, or <code>\"tr_bdf2\"</code>. See the solver table in the API overview for adjoint behaviour.</p> <code>'bdf'</code> Source code in <code>python/diffsol_jax/__init__.py</code> <pre><code>def __init__(\n    self,\n    rhs: Callable[\n        [Float[Array, \"\"], Float[Array, \" state\"], Float[Array, \" params\"]],\n        Float[Array, \" state\"],\n    ],\n    y0: Float[Array, \" state\"],\n    params: Float[Array, \" params\"],\n    n_times: int = 200,\n    ode_solver: ODE_SOLVER = \"bdf\",\n):\n    \"\"\"Compile an ODE problem from a Python RHS function.\n\n    :param rhs: Right-hand-side function ``rhs(t, y, p) -&gt; tuple[float, ...]``.\n        Must return a tuple of scalars, one per state component.  Supports\n        elementwise JAX arithmetic, common unary functions (``sin``, ``exp``,\n        …), and scalar indexing of ``y`` and ``p``.\n    :param y0: Initial state vector.  Shape determines ``n_state``.  Values\n        are baked into the compiled DiffSL source; gradient w.r.t. ``y0`` is\n        not returned by :attr:`solve`.\n    :param params: Example parameter vector used to trace ``rhs`` and infer\n        ``n_params``.  The values set the DiffSL default parameters and are\n        overridden at solve time.\n    :param n_times: Number of output time points (uniformly spaced between\n        ``t_span[0]`` and ``t_span[1]``).  Defaults to ``200``.\n    :param ode_solver: Which solver to use for the forward pass.  One of\n        ``\"bdf\"`` (default), ``\"tsit45\"``, ``\"esdirk34\"``, or ``\"tr_bdf2\"``.\n        See the solver table in the API overview for adjoint behaviour.\n    \"\"\"\n\n    diffsl_src = make_diffsl_tuple(rhs, y0, params)\n    solver = _rust.OdeSolver(diffsl_src)\n\n    self.solver = solver\n    self._handle = solver.handle()\n    self.n_times = n_times\n    solver_code = _method_code(ode_solver)\n\n    n_state = len(y0)\n\n    @jax.custom_vjp\n    def solve(\n        params: Float[Array, \" params\"],\n        t_span: Float[Array, \" 2\"],\n    ) -&gt; Tuple[Float[Array, \"{n_times}\"], Float[Array, \"{n_times} params\"]]:\n\n        (ts, ys), _ = fwd(params, t_span)\n\n        return ts, ys\n\n    def fwd(params, t_span):\n        ys, ts = _solve_forward(\n            solver.handle(),\n            params,\n            t_span[0],\n            t_span[1],\n            n_times,\n            n_state,\n            solver_code,\n        )\n        return (ts, ys), (params, t_span)\n\n    def bwd(res, g):\n        params, t_span = res\n        _, g_ys = g\n        grad_params, _grad_y0 = _solve_adjoint(\n            solver.handle(), params, t_span, g_ys, n_times, n_state, solver_code\n        )\n        return grad_params, jnp.zeros_like(t_span)\n\n    solve.defvjp(fwd, bwd)\n    self.solve = solve\n</code></pre>","path":["API reference","diffsol_jax"],"tags":[]}]}